根据您提供的开发文档，我已为您生成了更新版本的开发需求文档。新文档在原有基础上增加了**算法数据可视化**、**拼写练习功能**以及**单词下次复习时间可视化**三个核心模块，并调整了相关的数据库设计与交互逻辑。
---
# 项目名称：考研离线背单词助手 开发需求文档 (V2.0) - 安卓版
## 1. 项目概述
### 1.1 项目背景
目标用户为考研学生，核心痛点为：图书馆/自习室网络不稳定、需要专注静音学习、需要根据复习进度灵活切换词书。**V2.0版本旨在解决用户对学习效果的直观感知需求，以及强化单词拼写记忆能力。**
### 1.2 核心目标
1.  **安卓原生架构**：采用Kotlin语言，利用Jetpack组件构建高性能应用。
2.  **全离线运行**：无需网络权限，零依赖外部资源。
3.  **灵活词书切换**：支持多本词书及生词本无缝切换。
4.  **生词本管理**：提供独立的生词收集与学习入口。
5.  **学习数据可视化**：通过图表直观展示记忆曲线与学习成果。
6.  **拼写强化**：提供主动回忆式的拼写练习模式。
7.  **复习时间透明化**：用户可见每个单词的下次复习时间，增强掌控感。
### 1.3 技术栈
*   **开发语言**：Kotlin
*   **UI框架**：Jetpack Compose (推荐) 或 XML + ViewBinding
*   **数据库**：Room Database (SQLite封装)
*   **图表库**：MPAndroidChart 或 Compose-Charts (用于数据可视化)
*   **异步处理**：Kotlin Coroutines + Flow
*   **文件处理**：Android Storage Access Framework (SAF)
---
## 2. 功能模块详细设计 (更新)
### 2.1 词书管理与切换功能
*（保持原逻辑，新增词书维度的统计数据展示）*
*   **词书列表**：
    *   展示所有可用词书。
    *   **新增**：在词书Item上展示简要统计，如“已掌握率”、“预计复习单词数”。
### 2.2 生词本功能
*（保持原逻辑）*
### 2.3 核心学习流程 (更新)
*   **卡片展示**：
    *   大字号显示单词、音标。
    *   **新增：复习时间可视化**：
        *   在卡片角落显示一个小图标或标签（如“📅 3天后”或“今日复习”）。
        *   点击可查看具体复习规划（如：“上次复习：2天前，下次复习：明天”）。
*   **交互逻辑**：
    *   **认识**：进入下一词，调度算法推迟复习时间。
    *   **模糊/不认识**：显示释义详情，进入下一词，调度算法安排近期复习。
    *   **新增：拼写练习入口**：
        *   在学习模式选择中，增加“拼写模式”选项。
### 2.4 拼写练习功能 (新增)
此功能用于强化单词拼写记忆，支持“看义拼词”。
*   **练习入口**：
    *   学习主页顶部Tab切换：[认词模式] / [拼写模式]。
    *   或在词库页面针对特定词书发起“拼写测试”。
*   **交互逻辑**：
    *   **出题**：显示单词释义和音标，隐藏单词拼写。
    *   **作答**：弹出软键盘，用户输入框。支持实时纠错提示（如字母正确但位置错误变灰）。
    *   **判定**：
        *   **正确**：输入框变绿，播放微弱提示音（震动），自动跳下一词，并更新SM-2算法数据（视同“认识”）。
        *   **错误**：输入框变红，显示正确拼写对比，用户需手动点击“记住了”进入下一词，视同“模糊”处理。
    *   **提示功能**：提供“显示首字母”或“显示长度”辅助提示（扣分权重降低）。
### 2.5 学习数据可视化功能 (新增)
将枯燥的数据库记录转化为直观的图表，帮助用户建立信心。
*   **统计维度**：
    *   **记忆曲线图**：展示用户过去7天/30天的每日复习量与新词量折线图。
    *   **掌握度分布**：环形图展示当前词书中“未学”、“学习中”、“已掌握”单词的比例。
    *   **日历热力图**：类似GitHub贡献图，颜色深浅代表当天学习的单词数量。
*   **展示位置**：
    *   集中在“我的”页面展示总览。
    *   在各词书详情页展示该词书的独立统计。
---
## 3. 数据库设计
### 3.1 词书表
*（保持不变）*
*   `id`, `name`, `type`, `total_count`, `is_active`
### 3.2 单词表
*（保持不变）*
*   `id`, `word`, `phonetic`, `meaning`, `example`, `book_id`
### 3.3 学习进度表
*（增加字段以支持拼写记录）*
*   `id` (Long, Primary Key)
*   `word_id` (Long)
*   `book_id` (Long)
*   `status` (Int) - 0(新词), 1(学习中), 2(已掌握)
*   `next_review_time` (Long) - **关键：用于UI展示的复习时间**
*   `ease_factor` (Float) - 记忆因子
*   `review_count` (Int) - **新增**：总复习次数
*   `spell_correct_count` (Int) - **新增**：拼写正确次数
*   `spell_wrong_count` (Int) - **新增**：拼写错误次数
### 3.4 学习记录表 (新增)
*用于支持数据可视化图表*
*   `id` (Long, Primary Key)
*   `date` (Long) - 时间戳（精确到天）
*   `new_words_count` (Int) - 当天新学单词数
*   `review_words_count` (Int) - 当天复习单词数
*   `spell_practice_count` (Int) - 当天拼写练习数
*   `duration_millis` (Long) - 学习时长
### 3.5 生词本关联表
*（保持不变）*
*   `id`, `word_id`, `add_time`, `note`
---
## 4. 安卓专属UI/UX设计 (更新)
### 4.1 界面结构
采用单Activity多Fragment架构，底部导航栏包含四个Tab：
1.  **学习**：
    *   **顶部**：当前词书名称 + **模式切换Tabs (认词/拼写)**。
    *   **中部**：
        *   *认词模式*：单词卡片，**右上角显示“下次复习：X天后”**。
        *   *拼写模式*：显示释义卡片，底部输入框。
    *   **底部**：操作按钮。
2.  **查词**：
    *   搜索框 + 本地词典列表。
    *   搜索结果页含“加入生词本”图标，**显示该词个人掌握状态（如：已掌握，下次复习：无）**。
3.  **词库**：
    *   列表展示所有词书。
    *   点击词书Item进入详情页：
        *   **新增统计区**：展示该词书的环形图（掌握度）。
        *   功能按钮：[切换使用]、[拼写测试]、[删除]。
    *   右下角悬浮按钮：导入新词书。
4.  **我的**：
    *   **学习统计大屏 (核心更新)**：
        *   **日历热力图**：展示打卡记录。
        *   **趋势图**：过去一周学习量趋势。
    *   数据备份与恢复。
    *   设置。
### 4.2 交互细节
*   **复习时间点击**：点击单词卡上的“下次复习时间”，弹窗解释调度逻辑（例如：“根据SM-2算法，您对此词记忆强度较高，建议3天后复习”）。
*   **拼写反馈**：拼写错误时，使用红色文字高亮显示错误的字母位置。
---
## 5. 关键逻辑实现指南 (伪代码/Kotlin思路)
### 5.1 复习时间可视化逻辑
```kotlin
// ViewModel
val nextReviewText = meditatedWord.map { word ->
    val reviewTime = word.next_review_time
    if (reviewTime == 0L) "无需复习"
    else {
        val diffDays = TimeUnit.MILLISECONDS.toDays(reviewTime - System.currentTimeMillis())
        when {
            diffDays <= 0 -> "今日复习"
            diffDays == 1L -> "明天复习"
            else -> "${diffDays}天后复习"
        }
    }
}
// UI (Compose)
Text(text = nextReviewText, style = MaterialTheme.typography.caption)
```
### 5.2 拼写练习判定逻辑
```kotlin
fun checkSpelling(input: String, correctWord: String) {
    val isCorrect = input.trim().equals(correctWord, ignoreCase = true)
    
    if (isCorrect) {
        // 更新SM-2参数，增加间隔
        updateWordProgress(wordId, quality = 5) // 5 = 完美记忆
        _uiEvent.value = UiEvent.ShowCorrectAnimation
    } else {
        // 重置或缩短间隔
        updateWordProgress(wordId, quality = 2) // 2 = 错误
        _uiEvent.value = UiEvent.ShowWrongAnimation(correctWord)
    }
    
    // 记录到每日统计表
    repository.incrementDailySpellCount(isCorrect)
}
```
### 5.3 统计数据查询逻辑
```kotlin
// Repository
fun getWeeklyStats(): Flow<List<DailyStats>> {
    val calendar = Calendar.getInstance()
    val endTime = calendar.timeInMillis
    calendar.add(Calendar.DAY_OF_YEAR, -7)
    val startTime = calendar.timeInMillis
    
    return dailyStatsDao.getStatsBetween(startTime, endTime)
}
// DAO
@Query("SELECT * FROM tb_daily_stats WHERE date BETWEEN :start AND :end ORDER BY date ASC")
fun getStatsBetween(start: Long, end: Long): Flow<List<DailyStats>>
```
---
## 6. 交付清单 (更新)
请AI开发者生成一个完整的Android Studio工程，包含：
1.  **Model层**：
    *   Room数据库实体（**新增 DailyStats 实体**）。
    *   DAO接口（**新增 统计查询接口**）。
    *   Database类。
2.  **Repository层**：
    *   封装数据操作，处理词书切换、生词本逻辑、**拼写判定逻辑**。
3.  **ViewModel层**：
    *   管理UI状态（**含图表数据 StateFlow**），处理协程。
4.  **UI层**：
    *   `LearningScreen`：更新UI展示**复习时间标签**。
    *   `SpellingScreen`：**新增**拼写练习界面。
    *   `StatsScreen`：**新增**数据可视化界面（使用Charts组件）。
    *   `BookManageScreen`：含切换、导入功能。
5.  **Assets**：预置的 `vocabulary.db` 文件。
6.  **Utils**：
    *   文件解析工具类。
    *   **SM-2算法工具类**：需返回计算后的 `nextReviewTime` 用于UI展示。
    *   **DateUtils**：用于将时间戳转换为“X天后”等友好文案。
**特别说明：**
*   数据可视化图表需适配深色模式。
*   拼写功能需处理软键盘弹出导致的界面布局重排问题。
*   复习时间显示需考虑时区问题，统一使用UTC时间戳存储。
